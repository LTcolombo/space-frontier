{
  "name": "avatar-frontiergame",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "scripts": {
    "deploy": "cd contract && npm run deploy",
    "build": "npm run build:contract",
    "build:contract": "cd contract && npm run build",
    "postinstall": "cd contract && npm install"
  },
  "devDependencies": {
    "near-cli": "^3.3.0"
  },
  "dependencies": {}
}